name: 'dbt_adapter_smoke'
version: '1.0.0'
config-version: 2

profile: 'dbt_adapter_smoke'

model-paths: ['models']
target-path: 'target'
log-path: 'logs'
packages-install-path: 'dbt_packages'
clean-targets:
  - 'target'
  - 'dbt_packages'

models:
  dbt_adapter_smoke:
    +materialized: view
